TODO: drop the FlashWorld-generated `.ply` (and `.spz`/preview `.mp4` if kept) here once
Kaggle generation (H0) is done. `*.ply`/`*.spz`/`*.mp4` are gitignored — this directory
stays empty in version control until an artifact is placed locally.
