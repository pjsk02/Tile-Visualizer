Owner: **SHARED / FROZEN**

`catalog.json` is a copy of the root-level `catalog.json` produced by
`scripts/extract_catalog.py` (49 glossy tiles). Do not hand-edit; do not regenerate from
here — re-run the extraction script at the repo root and re-copy if the source PDF
changes.
