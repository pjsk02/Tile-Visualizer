Owner: **SHARED / FROZEN**

`catalog.ts` (`Tile`/`Design`) and `surface.ts` (`Surface`) are the frozen data
contracts. `catalog.ts` must keep matching `src/data/catalog.json` exactly — change only
by agreement between both devs.
