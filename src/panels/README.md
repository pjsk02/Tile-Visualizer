Owner: **Dev A**

Inserted flat-quad panels (the only things ever retextured — splats are never retextured
directly, see root CLAUDE.md). Reads `selectedDesign` from `useVisualizerStore` to swap a
panel's material/texture.
